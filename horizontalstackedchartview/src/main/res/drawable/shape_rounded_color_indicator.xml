<!-- shape_rounded_color_indicator.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Change to the desired background color -->
    <corners android:radius="10dp" /> <!-- Adjust the radius to control the roundness of corners -->
    <size android:width="20dp" android:height="20dp" /> <!-- Adjust the size of the shape -->
</shape>
